#include<stdio.h>
int main()
{
int arr[10], i, n, sum=0;
printf("Enter the size of the array:");
scanf("%d",&n);
printf("Enter the elements in a array:"\n);
for(i=0;i<n;i++)
{
scanf("%d",&arr[i]);
sum=sum+arr[i];
}
printf("Sum = %d",sum);
return 0;
}
