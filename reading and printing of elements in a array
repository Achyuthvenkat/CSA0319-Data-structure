#include<stdio.h>
int main()
{
 int arr[5], n, i;
  printf("Enter the size of an array:");
  scanf("%d",&n);
  printf("Enter the elements in a array:\n");
  for(i=0;i<n;i++)
    {
    scanf("%d",&arr[i]);
    }
  for(i=0;i<n;i++)
    printf("%d",arr[i]);
  return 0;
